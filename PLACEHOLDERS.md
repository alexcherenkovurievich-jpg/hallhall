# Изображения

Реальных кадров пять, вставлены без обработки, как есть
(`public/images/photos/`):

| Файл | Блок |
|---|---|
| `01-hero-pool.jpg`        | Первый экран — фон под затемнением |
| `02-about-panorama.jpg`   | О месте — широкий кадр под заголовком |
| `03-house.jpg`            | Дом — главный кадр |
| `04-house-interior-1.jpg` | Дом — интерьер 1 |
| `05-house-interior-2.jpg` | Дом — интерьер 2 |

Остальные кадры — заглушки. Ниже что и в какой пропорции нужно снять.
Пропорция важна: под неё сверстана сетка, реальное фото встанет
без прыжков вёрстки.

| # | Секция | Кадр | Пропорция |
|---|--------|------|-----------|
| 1 | Дом | Интерьер 3 | 1:1 квадрат |
| 2 | Территория | Веранда / беседка | 4:3 |
| 3 | Территория | Мангалы, печь под казан | 4:3 |
| 4 | Территория | Костровая чаша вечером | 4:3 (тёмный кадр, тёплый свет) |
| 5 | Услуги | Русская баня | 4:3, верх карточки |
| 6 | Услуги | Банный чан | 4:3, верх карточки |
| 7 | Услуги | Бассейны | 4:3, верх карточки |
| 8 | Галерея | 10 кадров | 4:5, сетка 5×2 на десктопе и 2×5 на мобильном |
| 9 | Отзывы | Скриншоты отзывов, 7 шт | 3:4 вертикальные |
| 10 | Контакты | Вечерний двор | 4:5 вертикальный |

## Как подставлять

Заглушка — это `<figure class="ph t-цвет" style="--ar:4/3" data-label="...">`.
Реальное фото — то же `<figure>`, но с классом `ph--photo` и `<img>` внутри:

```html
<figure class="ph ph--photo" style="--ar:4/3;--pos:center 70%">
  <img src="../public/images/photos/файл.jpg" loading="lazy" decoding="async"
       width="..." height="..." alt="что на кадре">
</figure>
```

`--pos` сдвигает кадрирование, когда пропорция фото не совпадает с рамкой
(`center 70%` тянет видимую область к низу). Вертикальный кадр в широком
блоке лучше не растягивать — для этого есть класс `ph--tall`, он ставит
фото по центру и ограничивает ширину.

## Разрешение

Все присланные кадры проходят через сжатие мессенджера. Если есть
оригиналы с телефона — их лучше положить вместо текущих: первый экран
растягивает фото на всю ширину экрана, и там запас по разрешению виден.

## Порядок секций

Карта («Расположение») стоит перед «Контактами» — сразу после «Отзывов».

## Карта

Яндекс.Карта через Yandex Maps JavaScript API. Ключ, координаты и зум —
константами в начале `<script>` внизу `proto.html`:

```js
const YANDEX_MAPS_API_KEY = '...';
const VENUE_COORDS = [54.272215, 48.248568];   // [широта, долгота]
const VENUE_ZOOM   = 16;
```

Зум колёсиком отключён, кнопки зума оставлены, метка неперетаскиваемая.
Если API не загрузился, вместо карты показывается адрес и ссылка
на Яндекс.Карты. Под картой — кнопка «Открыть в Яндекс.Картах»
(в её ссылке порядок координат обратный: долгота, широта).
